import styled from 'styled-components';
import { fieldStyles, errorStyles } from '../styled';

export const inputErrorStyles = errorStyles;

export const StyledInput = styled.input`
  ${fieldStyles}
`;
